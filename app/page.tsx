import Link from 'next/link';
import {getPopulerHesaplamalar,kategoriler,hesaplamalar} from '@/lib/hesaplamalar';
export default function HomePage(){
  const populerler=getPopulerHesaplamalar();
  return(<div className="min-h-screen bg-gradient-to-br from-blue-50 via-white to-indigo-50"><div className="relative overflow-hidden bg-gradient-to-r from-primary-600 via-blue-600 to-indigo-600 text-white"><div className="relative max-w-7xl mx-auto px-4 py-24 text-center"><h1 className="text-5xl md:text-6xl font-bold mb-6">Türkiye'nin En Kapsamlı<br/>Hesaplama Platformu</h1><p className="text-xl md:text-2xl mb-8 text-blue-100 max-w-3xl mx-auto">{hesaplamalar.length}+ ücretsiz hesaplama aracı. YKS, KPSS, kredi ve daha fazlası.</p><div className="flex flex-col sm:flex-row gap-4 justify-center"><Link href="/hesaplamalar/yks-puan" className="px-8 py-4 bg-white text-primary-600 rounded-xl font-bold hover:bg-blue-50">🎓 YKS Puan Hesapla</Link><Link href="/hesaplamalar" className="px-8 py-4 bg-white/10 text-white border-2 border-white rounded-xl font-bold hover:bg-white/20">Tüm Araçlar →</Link></div></div></div><div className="max-w-7xl mx-auto px-4 py-16"><div className="grid grid-cols-3 gap-6 mb-20">{[{l:hesaplamalar.length+'+',t:'Hesaplama'},{l:'100%',t:'Ücretsiz'},{l:'0',t:'Kayıt Yok'}].map((s,i)=>(<div key={i} className="text-center"><div className="text-5xl font-bold text-primary-600 mb-2">{s.l}</div><div className="text-gray-600">{s.t}</div></div>))}</div><h2 className="text-3xl font-bold mb-8 text-center">🔥 En Popüler</h2><div className="grid md:grid-cols-4 gap-6 mb-20">{populerler.slice(0,8).map(h=>(<Link key={h.id} href={`/hesaplamalar/${h.id}`} className="hesaplama-card"><div className="text-4xl mb-4">{h.icon}</div><h3 className="font-bold text-lg mb-2">{h.baslik}</h3><p className="text-sm text-gray-600">{h.aciklama}</p></Link>))}</div><h2 className="text-3xl font-bold mb-8 text-center">📂 Kategoriler</h2><div className="grid md:grid-cols-5 gap-4">{kategoriler.map(k=>(<Link key={k.id} href={`/hesaplamalar?kategori=${k.id}`} className="p-6 bg-white rounded-xl shadow-sm hover:shadow-xl text-center"><div className="text-4xl mb-3">{k.icon}</div><div className="font-semibold">{k.baslik}</div></Link>))}</div></div></div>);
}
